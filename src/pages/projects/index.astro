---
import TerminalPanel from '../../components/TerminalPanel.astro';
import TrackCard from '../../components/TrackCard.astro';
import { tracks } from '../../data/tracks';
import SiteLayout from '../../layouts/SiteLayout.astro';

const latestTrack = tracks[0]!;
const playerUrl = `https://w.soundcloud.com/player/?url=${encodeURIComponent(latestTrack.url)}&color=%235f8c44&auto_play=false&hide_related=true&show_comments=false&show_user=true&show_reposts=false&show_teaser=false`;
---

<SiteLayout
  title="Music"
  description="Listen to my electronic tracks, flips and covers."
  image={latestTrack.artwork}
>
  <div class="site-stack projects-stack">
    <h1 class="sr-only">Music</h1>

    <TerminalPanel title="Latest upload" class="featured-player" shear="none">
      <div class="player-layout">
        <div>
          <h2>{latestTrack.title}</h2>
          <p>kashii · {latestTrack.genre} · {latestTrack.format} · {latestTrack.releaseDate}</p>
          <a href={latestTrack.url} target="_blank" rel="noreferrer">Open track on SoundCloud <span aria-hidden="true">↗</span></a>
        </div>
        <iframe
          title={`SoundCloud player for ${latestTrack.title}`}
          width="100%"
          height="166"
          allow="autoplay"
          loading="eager"
          src={playerUrl}
        ></iframe>
      </div>
    </TerminalPanel>

    <section aria-labelledby="catalogue-title">
      <div class="section-heading">
        <h2 id="catalogue-title">All tracks</h2>
        <p>{tracks.length} public uploads · 2025—2026</p>
      </div>
      <div class="track-grid">
        {tracks.map((track, index) => <TrackCard track={track} index={index} priority={index < 3} />)}
      </div>
    </section>

  </div>
</SiteLayout>

<style>
  .projects-stack {
    gap: clamp(3rem, 7vw, 5.5rem);
  }

  .player-layout {
    display: grid;
    grid-template-columns: minmax(13rem, 0.65fr) minmax(18rem, 1.35fr);
    gap: clamp(1.25rem, 4vw, 3rem);
    align-items: center;
  }


  .player-layout h2 {
    margin-block: 0.6rem 0;
    font-size: clamp(1.7rem, 4vw, 2.7rem);
    line-height: 1;
    letter-spacing: -0.055em;
  }

  .player-layout p {
    margin-block: 0.55rem 1.1rem;
    color: var(--foreground2);
  }

  .player-layout a {
    color: var(--accent-soft);
    font-weight: 700;
  }

  .player-layout iframe {
    background: var(--background0);
  }


  @media (max-width: 760px) {
    .player-layout {
      grid-template-columns: 1fr;
    }

    .projects-stack {
      gap: 2.5rem;
    }
  }
</style>
